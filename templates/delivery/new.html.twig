{% extends 'base.html.twig' %}

{% block title %}Nouvelle commande{% endblock %}

{% block body %}
    <div class="container mt-4" style="width:40rem;">
        <h1 class="mb-4">Créer une nouvelle commande</h1>

        <div class="card">
            <div class="card-header">
                <h5 class="m-0">Formulaire de création</h5>
            </div>
            <div class="card-body">
                {{ include('delivery/_form.html.twig') }}
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('app_delivery_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}