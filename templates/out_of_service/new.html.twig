{% extends 'base.html.twig' %}

{% block title %}Nouveau OutOfService{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Mise hors service</h1>

        <div class="card">
            <div class="card-header">
                <h5 class="m-0">Formulaire de mise hors service</h5>
            </div>
            <div class="card-body">

        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <strong>ID:</strong>
                        <p>{{ delivery.id }}</p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <strong>Code de Livraison:</strong>
                        <p>{{ delivery.deliveryCode }}</p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <strong>Date:</strong>
                        <p>{{ delivery.date ? delivery.date|date('Y-m-d') : 'N/A' }}</p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <strong>Range 1:</strong>
                        <p>{{ delivery.range1 }}</p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <strong>Range 2:</strong>
                        <p>{{ delivery.range2 }}</p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <strong>Total de la Carte:</strong>
                        <p>{{ delivery.cardTotal }}</p>
                    </div>
                </div>
            </div>
        </div>

                {{ include('out_of_service/_form.html.twig') }}
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('app_out_of_service_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}