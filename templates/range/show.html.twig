{% extends 'base.html.twig' %}

{% block title %}Range{% endblock %}

{% block body %}
    <h1>Range</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ range.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ range.start }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ range.end }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ range.createdAt ? range.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_range_index') }}">back to list</a>

    <a href="{{ path('app_range_edit', {'id': range.id}) }}">edit</a>

    {{ include('range/_delete_form.html.twig') }}
{% endblock %}
