{% extends 'base.html.twig' %}

{% block title %}Edit Bin{% endblock %}

{% block body %}
    <div class="container mt-4" style="width:40rem;">
        <h1 class="mb-4">Modifier le Bin</h1>

        <div class="card">
            <div class="card-header">
                <h5 class="m-0">Formulaire de modification</h5>
            </div>
            <div class="card-body">
                {{ include('bin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('app_bin_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>

        {{ include('bin/_delete_form.html.twig') }}
    </div>
{% endblock %}