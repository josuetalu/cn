<form method="post" action="{{ path('app_bin_delete', {'id': bin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bin.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
