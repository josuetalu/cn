{% extends 'base.html.twig' %}

{% block title %}New Bin{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Créer un nouveau Bin</h1>

        <div class="card">
            <div class="card-header">
                <h5 class="m-0">Formulaire de création</h5>
            </div>
            <div class="card-body">
                {{ include('bin/_form.html.twig') }}
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('app_bin_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}