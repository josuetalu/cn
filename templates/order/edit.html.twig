{% extends 'base.html.twig' %}

{% block title %}Modification d'une commande{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Modifier une commande</h1>

        <div class="card">
            <div class="card-header">
                <h5 class="m-0">Formulaire de modification</h5>
            </div>
            <div class="card-body">
                 {{ include('order/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
            <div class="card-footer text-end">
                <a class="btn btn-secondary" href="{{ path('app_order_index') }}">Retour à la liste</a>
                <br/>
                <br/>
                {{ include('order/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
